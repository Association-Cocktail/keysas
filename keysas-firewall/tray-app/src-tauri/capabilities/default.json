{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability — grants the main window access to core Tauri APIs",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
